# API

## Scripts

```bash
npx sequelize-cli model:generate --name user --attributes username:string,email:string,password:string,age:integer

npx sequelize-cli model:generate --name item --attributes name:string,type:string,price:integer,image:string,userId:integer
```
